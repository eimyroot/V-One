# Policy Decision Boundary

V-One policy decisions are contract records for authorization outcomes. The allowed outcomes are `allow`, `deny`, `blocked`, and `needs_approval`.

Every policy decision must carry purpose, system benefit, input digest, policy bundle digest, evidence-backed reasons, and a deterministic decision digest. Unknown outcomes fail closed.
