# Canonical product source

This directory represents the V-One canonical product tree:

`nulleimy/V-One:docs/product@main`

Current-state reads should resolve from GitHub and be pinned to the exact selected commit SHA when reproducibility or evidence matters.

This CASER/GitHub mirror layer is intentionally **not** a second mutable source of truth.

Canonical product content remains under `docs/product`.
